#include "subscribedispatcher.h"

/*static SubscribeDispatcher::SubscribeDispatcher* instance()
{
    if(!pInstance)
    {
        pInstance = new SubscribeDispatcher;
    }
}*/

SubscribeDispatcher::SubscribeDispatcher()
{
  //  subscribers.insert("GPS","");
    //subscribers.insert("Hello","");
}

